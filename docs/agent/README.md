# Agent Interaction Records

This directory stores only authentic interaction text available during development.

No Gate 0–5 conversation was reconstructed from Git history, commit messages, or the current implementation. The commit map in `INDEX.md` is provenance metadata, not a transcript. Exact older transcripts may be added later only if their original text is supplied.

Gate 6 contains the received task prompt and the result recorded after verification.
